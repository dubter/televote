// Package migrations хранит схему Postgres.
//
// Файлы встроены в бинарь: мигратор в контейнере не должен зависеть от того,
// смонтирован ли каталог, и от того, совпадает ли его содержимое с версией
// собранного образа.
package migrations

import "embed"

// FS — все SQL-миграции проекта.
//
//go:embed *.sql
var FS embed.FS
